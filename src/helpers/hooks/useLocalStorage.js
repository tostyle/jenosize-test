import { useEffect } from "react";

const useLocalStorage = (savedState, dependencies) => {
  // useEffect(() => {
  // }, dependencies)
  // Object.keys(savedState).forEach(stateKey => {
  //   const stateValue = savedState[stateKey]
  //   localStorage.setItem(stateKey, stateValue)
  // })
};

export default useLocalStorage;
